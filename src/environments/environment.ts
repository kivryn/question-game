// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCm16IAm3-43W8geC6zCW30bmTNvu2Myl4",
    authDomain: "kiv-question-game.firebaseapp.com",
    databaseURL: "https://kiv-question-game.firebaseio.com",
    projectId: "kiv-question-game",
    storageBucket: "kiv-question-game.appspot.com",
    messagingSenderId: "633369204762",
    appId: "1:633369204762:web:7c50a5689403aaa75bd9a3"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
